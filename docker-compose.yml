services:
  blackvault:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./app/static/uploads:/app/app/static/uploads
      - ./instance:/app/instance
      - ./blackvault.db:/app/blackvault.db
    env_file:
      - .env
    restart: unless-stopped
